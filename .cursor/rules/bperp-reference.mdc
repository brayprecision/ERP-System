---
description: Key file paths and common shell commands for BPERP
alwaysApply: true
---

# BPERP — Quick reference

## Key paths

| Component | Path |
|-----------|------|
| Backend entry | `backend/server.js` |
| Backend routes (JS) | `backend/routes/*.js` |
| Middleware | `backend/middleware/*.js` |
| Type definitions | `backend/src/types/index.ts` |
| Frontend entry | `frontend/js/app.js` |
| Frontend modules | `frontend/js/modules/*.js` |
| Frontend CSS | `frontend/css/dashboard.css` |
| Electron main | `electron/main.js` |
| Electron preload | `electron/preload.js` |
| Setup wizard | `electron/setup-wizard/` |
| Electron native rebuild | `scripts/rebuild-backend-native.js` |
| Database schemas | `database/*.sql` |
| Build config | `package.json` (build section) |
| Cursor rules | `.cursor/rules/*.mdc` |

## Common commands

```bash
# Development
cd backend && npm run dev          # Backend with nodemon (system Node)
npm run rebuild:backend            # Repo root: native modules for Electron
npm start                          # Electron (forks backend; run rebuild:backend after backend npm install)

# Testing
cd backend && npm test

# Building installers (from repo root; runs backend:install:prod + rebuild:backend + electron-builder)
npm run build:win                    # Windows NSIS + unpacked → dist-installers/
npm run build:linux                  # Linux AppImage + deb (no rpm)
npm run pack:win                     # Windows unpacked only (faster)

# Test unpacked Windows build
# dist-installers/win-unpacked/BPERP.exe

# Beta (Windows): repack + run unpacked — `.\scripts\launch-beta.ps1` or `scripts\launch-beta.cmd`
# After packaging, run `npm run backend:install` if you need backend dev deps (tests, nodemon) again.

# Database
cd backend && npm run migrate
```
