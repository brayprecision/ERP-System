# Test Environment Configuration
# Copy to .env.test and update for your test database

NODE_ENV=test

# Test Database (should be separate from development/production!)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bperp_test
DB_USER=postgres
DB_PASSWORD=password

# Server (not used in tests, but may be needed)
PORT=3001
